PORT=4000
JWT_SECRET=changeme_usar_minimo_32_chars_random
DATABASE_URL=postgres://user:password@localhost:5432/nutricare

# URL del frontend (para el enlace de recuperación de contraseña)
FRONTEND_URL=http://localhost:5173

# Orígenes permitidos en CORS (separados por coma). Usar * solo en desarrollo.
CORS_ORIGIN=http://localhost:5173

# Configuración SMTP para emails (dejar vacío para modo consola en desarrollo)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=tu_email@gmail.com
SMTP_PASS=tu_app_password
SMTP_FROM=Nutricare <tu_email@gmail.com>
